* export blender animations
  * on renderer: initially assume 1:1 node/action mapping
    v update EntityFile.txt doc
    v update exporter
    v update importer
    v render (add frame parameter to Entity::render)
  v figure when one action can affect multiple nodes
  v figure out when multiple actions can affect the same node or groups of nodes
v better shader
v shadow mapping
v animated gif
v batch meshes in renderer
* exporter: use FCurve::array_index to figure out component index (e.g. X/Y/Z)
